"""Channel Lens — a local YouTube strategy workbench.

Runs entirely on your machine, talks to YouTube with your own API key, and
keeps every byte it fetches in a local SQLite database so the same data is
never paid for twice.
"""

__version__ = "0.1.0"
